# Qwen2.5-7B-Instruct - Large text-only LLM
# ~5GB VRAM (4-bit), ~14GB (FP16)
# High quality output

name: "Qwen2.5-7B-Instruct"
model_id: "Qwen/Qwen2.5-7B-Instruct"
model_type: "qwen25_text"
dtype: "bfloat16"

download:
  subfolder: "LLM"
  use_symlinks: false

generation:
  max_tokens: 512
  do_sample: true
  temperature: 0.7
  top_p: 0.9
